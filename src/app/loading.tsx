import LoadingSpinner from '@/components/shared/LoadingSpinner';

export default function Loading() {
  // Default to creative design for loading state
  return <LoadingSpinner design="creative" />;
}
